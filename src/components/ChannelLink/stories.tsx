import { Story, Meta } from '@storybook/react'
import ChannelLink from '.'

export default {
  title: 'ChannelLink',
  component: ChannelLink
} as Meta

export const Default: Story = () => <ChannelLink />
